apiVersion: v1
kind: Service
metadata:
  name: postgres-learn
  namespace: default
  labels:
    app: postgres-learn
    env: production
spec:
  type: ClusterIP
  selector:
    app: postgres-learn
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres